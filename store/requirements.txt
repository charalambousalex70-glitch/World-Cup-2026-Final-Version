fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
# Backs pydantic's EmailStr, used to validate the checkout email.
email-validator==2.2.0

# Card payments. Stripe hosts the payment page, so card numbers never reach us.
stripe==11.4.1

# Postgres (Railway) and SQLite (local dev, zero setup) async drivers.
asyncpg==0.30.0
aiosqlite==0.20.0

