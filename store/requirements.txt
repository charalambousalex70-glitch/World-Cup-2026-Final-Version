fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1

# Postgres (Railway) and SQLite (local dev, zero setup) async drivers.
asyncpg==0.30.0
aiosqlite==0.20.0

# --- Phase 2 (guest checkout) — uncomment when we get there ---
# stripe==11.4.1
# boto3==1.35.90          # Cloudflare R2 speaks the S3 API
