[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -q --strict-markers
markers =
    e2e: browser tests; need the server running (see tests/test_e2e_smoke.py)
